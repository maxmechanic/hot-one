{
  "name": "hot-one",
  "version": "1.1.0",
  "description": "Utility functions for detecting hot ones, and distance from the midday sun.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "tsc && ava",
    "clean": "rimraf dist/",
    "prepublish": "npm run clean && npm test && npm run build"
  },
  "author": "maxmechanic",
  "license": "MIT",
  "devDependencies": {
    "@types/geojson": "^1.0.2",
    "@types/node": "^8.0.7",
    "ava": "^0.25.0",
    "rimraf": "^2.6.1",
    "typescript": "^2.4.1"
  },
  "dependencies": {
    "@turf/turf": "^4.5.2",
    "date-fns": "^1.28.5",
    "suncalc": "^1.8.0"
  }
}
